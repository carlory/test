# test

A scratch repo for exercising the `no-mistakes` validation pipeline.

## Usage

```python
from string_utils import slugify, title_case

slugify("Hello World")      # -> 'hello-world'
title_case("hello world")   # -> 'Hello World'
```

## Development

Run the tests with the standard library:

```sh
python3 -m unittest -v
```
